% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd_dose_optimise.R
\name{dmd_dose_optimise}
\alias{dmd_dose_optimise}
\title{Find the cheapest or minimum-item combination for a clinical dose}
\usage{
dmd_dose_optimise(
  query,
  dose,
  dose_unit = NULL,
  db = dmdprices::dmd_master,
  method = c("partial", "exact", "fuzzy"),
  max_dist = 3,
  price = c("basic_price", "nhs_indicative_price"),
  objective = c("cheapest", "min_items"),
  preparation = NULL,
  active_only = TRUE,
  can_split = TRUE,
  can_split_vials = FALSE
)
}
\arguments{
\item{query}{Character string passed through to \code{\link[=dmd_price_lookup]{dmd_price_lookup()}}.}

\item{dose}{Numeric dose value (in \code{dose_unit}), \strong{or} a
self-contained dose string such as \code{"250 mg"}, \code{"250mg"}, or
\code{"0.25 g"}. When a string is supplied \code{dose_unit} may be omitted.}

\item{dose_unit}{One of \code{"mg"}, \code{"microgram"} / \code{"mcg"}, \code{"g"}, \code{"ml"},
\code{"unit"}. Default \code{"mg"}. Ignored (with a warning) if \code{dose} is a
string that already contains a unit.}

\item{db}{A \verb{<dmd_db>} object from \code{\link[=dmd_load]{dmd_load()}} or a tibble in the
same shape as \link{dmd_master}. Default: bundled \link{dmd_master}.}

\item{method, max_dist, active_only}{Passed through to \code{\link[=dmd_price_lookup]{dmd_price_lookup()}}.}

\item{price}{Which price column to use — \code{"basic_price"} (default) or
\code{"nhs_indicative_price"}. Falls back to the other column when the chosen
one is NA for an individual AMPP (a note is added).}

\item{objective}{Character vector of one or more objectives: \code{"cheapest"},
\code{"min_items"}, \code{"most_expensive"}. Pass \code{"all"} as a shorthand for all
three. Defaults to \code{c("cheapest", "min_items")}. Each objective produces
one row per preparation group in the result.}

\item{preparation}{Optional character — a case-insensitive plain substring
matched against \code{preparation_group} or \code{preparation_label} before
returning results. An exact key (e.g. \verb{\\"tablet|none|oral\\"}) continues to
work, but partial strings such as \verb{\\"infusion\\"} or \verb{\\"oral\\"} are also
accepted and will match any group whose key or label contains that text.
Pipe characters in preparation keys are treated literally, not as regex
alternation.}

\item{can_split}{Logical. \code{TRUE} (default) assumes that individual items
(tablets, capsules) can be taken from a part-pack, as is normal in
hospital dispensing. \code{FALSE} requires whole packs to be dispensed, as
is normal in community pharmacy. Concentration-based preparations
(liquids, inhalers, vials) are \strong{always} treated as unsplittable
regardless of this setting. When \code{can_split = FALSE}, reported costs
are whole-pack costs rather than pro-rata costs, and a
\code{"no-pack-splitting"} note is added.}

\item{can_split_vials}{Logical. If \code{TRUE}, concentration-based preparations
(vials, ampoules) may be costed as a fraction of a container (vial
sharing). Defaults to \code{FALSE}, which costs whole containers only.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per
\verb{(preparation_group, objective)} combination. See the package vignette for
the column layout. The \code{combination} column is a list of tibbles — one
row per AMPP picked, identifying the specific branded product(s) used.
\code{dose_cost_pence} is the cost (in pence) of supplying the requested dose:
pro-rata item cost when \code{can_split = TRUE} (hospital), or whole-pack cost
when \code{can_split = FALSE} (community). In the \code{combination} tibble, \code{count}
is the number of discrete dispensing units: individual tablets/capsules/
containers when \code{can_split = TRUE}, or whole packs when
\code{can_split = FALSE}.
}
\description{
Given a dose (e.g. 900 mg), searches the dm+d for products matching \code{query}
and returns the cheapest combination of AMPPs that delivers that dose,
and/or the combination using the fewest items (tablets, ampoules, etc.).
}
\details{
Products are segregated into preparation groups automatically so that, e.g.,
immediate-release and modified-release tablets are optimised separately and
never mixed within a single combination. For each group, one row per objective
is returned.
}
\examples{
\dontrun{
# Cheapest and minimum-item combinations for a 900 mg dose of metformin
dmd_dose_optimise("metformin", dose = 900, dose_unit = "mg")

# Equivalent: pass dose as a single string
dmd_dose_optimise("metformin", dose = "900 mg")
dmd_dose_optimise("metformin", dose = "0.9 g")   # same dose, different unit

# Only modified-release tablets
dmd_dose_optimise(
  "metformin", dose = 1500, dose_unit = "mg",
  preparation = "tablet|modified-release|oral"
)

# Community pharmacy — whole packs must be dispensed
dmd_dose_optimise("metformin", dose = "1500 mg", can_split = FALSE)

# Worst-case cost (most expensive AMPPs)
dmd_dose_optimise("metformin", dose = 1000, dose_unit = "mg",
                  objective = "most_expensive")

# All three objectives at once
dmd_dose_optimise("metformin", dose = 1000, dose_unit = "mg",
                  objective = "all")

# Vial sharing for IV preparations
dmd_dose_optimise("rituximab", dose = 250, dose_unit = "mg",
                  preparation = "infusion", can_split_vials = TRUE)
}
}
\seealso{
\code{\link[=dmd_price_lookup]{dmd_price_lookup()}}, \code{\link[=dmd_parse_strength]{dmd_parse_strength()}}
}
