% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd_dose_optimise.R
\name{dmd_dose_optimise}
\alias{dmd_dose_optimise}
\title{Find the cheapest or minimum-item combination for a clinical dose}
\usage{
dmd_dose_optimise(
  query,
  dose,
  dose_unit = c("mg", "microgram", "mcg", "g", "ml", "unit"),
  db = dmdprices::dmd_master,
  method = c("partial", "exact", "fuzzy"),
  max_dist = 3,
  price = c("basic_price", "nhs_indicative_price"),
  objective = c("both", "cheapest", "min_items"),
  preparation = NULL,
  active_only = TRUE
)
}
\arguments{
\item{query}{Character string passed through to \code{\link[=dmd_price_lookup]{dmd_price_lookup()}}.}

\item{dose}{Numeric dose value (in \code{dose_unit}).}

\item{dose_unit}{One of \code{"mg"}, \code{"microgram"} / \code{"mcg"}, \code{"g"}, \code{"ml"},
\code{"unit"}. Default \code{"mg"}.}

\item{db}{A \verb{<dmd_db>} object from \code{\link[=dmd_load]{dmd_load()}} or a tibble in the
same shape as \link{dmd_master}. Default: bundled \link{dmd_master}.}

\item{method, max_dist, active_only}{Passed through to \code{\link[=dmd_price_lookup]{dmd_price_lookup()}}.}

\item{price}{Which price column to use — \code{"basic_price"} (default) or
\code{"nhs_indicative_price"}. Falls back to the other column when the chosen
one is NA for an individual AMPP (a note is added).}

\item{objective}{\code{"both"} (default), \code{"cheapest"}, or \code{"min_items"}.}

\item{preparation}{Optional character — a preparation-group key (e.g.
\code{"tablet|none|oral"}) or label to filter groups before returning.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per
\code{(preparation_group, objective)} combination. See the package vignette for
the column layout. The \code{combination} column is a list of tibbles — one
row per AMPP picked, identifying the specific branded product(s) used.
}
\description{
Given a dose (e.g. 900 mg), searches the dm+d for products matching \code{query}
and returns the cheapest combination of AMPPs that delivers that dose,
and/or the combination using the fewest items (tablets, ampoules, etc.).
}
\details{
Products are segregated into preparation groups automatically so that, e.g.,
immediate-release and modified-release tablets are optimised separately and
never mixed within a single combination. For each group, up to two rows are
returned — one per objective.
}
\examples{
\dontrun{
# Cheapest and minimum-item combinations for a 900 mg dose of metformin
dmd_dose_optimise("metformin", dose = 900, dose_unit = "mg")

# Only modified-release tablets
dmd_dose_optimise(
  "metformin", dose = 1500, dose_unit = "mg",
  preparation = "tablet|modified-release|oral"
)
}
}
\seealso{
\code{\link[=dmd_price_lookup]{dmd_price_lookup()}}, \code{\link[=dmd_parse_strength]{dmd_parse_strength()}}
}
