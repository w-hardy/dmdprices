% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd_dose_optimise.R
\name{dmd_dose_cost_range}
\alias{dmd_dose_cost_range}
\title{Vectorised dose cost range lookup}
\usage{
dmd_dose_cost_range(
  query,
  dose,
  dose_unit = NULL,
  db = dmdprices::dmd_master,
  method = c("partial", "exact", "fuzzy"),
  max_dist = 3,
  price = c("basic_price", "nhs_indicative_price"),
  preparation = NULL,
  ingredient = NULL,
  active_only = TRUE,
  can_split = TRUE,
  can_split_vials = FALSE,
  over_delivery = c("forbid", "minimise", "allow"),
  na_value = NA_real_
)
}
\arguments{
\item{query, dose_unit, db, method, max_dist, price, preparation, ingredient, active_only, can_split}{As in \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}.}

\item{dose}{A \strong{numeric vector} of dose values in \code{dose_unit}. \code{NA}, zero,
or negative elements yield \code{na_value} in both output columns.}

\item{can_split_vials}{As in \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}. If \code{TRUE}, vials and
ampoules are costed as a fraction of a container (vial sharing).}

\item{over_delivery}{As in \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}. Defaults to \code{"forbid"}, so
doses that no combination delivers exactly return \code{na_value} (with one
warning per call) rather than the cost of an over-delivered dose. Pass
\code{"minimise"} or \code{"allow"} to cost over-delivering combinations.}

\item{na_value}{Scalar returned for doses that are \code{NA}, non-positive, or
for which no solution is found. Default \code{NA_real_}.}
}
\value{
A \link[tibble:tibble]{tibble} with \code{length(dose)} rows and two columns:
\describe{
\item{\code{lo_pence}}{Cheapest achievable dose cost in pence. Divide by 100
for GBP.}
\item{\code{hi_pence}}{Most expensive achievable dose cost in pence. Divide
by 100 for GBP.}
}
Both columns are \code{na_value} when no solution is found.
}
\description{
Returns the \strong{cheapest} and \strong{most expensive} achievable cost for each
dose in a single call. A purpose-built alternative to calling
\code{\link[=dmd_dose_cost]{dmd_dose_cost()}} twice with different \code{objective} values, with clearer
naming for health-economics range analyses.
}
\details{
The candidate preparation step is memoized, so even though this function
runs the DP twice internally (once per bound), the expensive price-lookup
and parsing work is only performed once per \verb{(query, db, ...)} combination
within a session.
}
\examples{
\dontrun{
# Cost range for rituximab doses — divide by 100 for GBP
library(dplyr)
doses_mg <- c(375, 500, 700)
dmd_dose_cost_range(
  query       = "rituximab",
  dose        = doses_mg,
  dose_unit   = "mg",
  preparation = "infusion"
) / 100

# Use inside mutate() to add lo/hi cost columns to a treatment table
treatment_df |>
  dplyr::bind_cols(
    dmd_dose_cost_range("rituximab", dose = treatment_df$dose_mg) / 100
  )
}
}
\seealso{
\code{\link[=dmd_dose_cost]{dmd_dose_cost()}} for a single-objective numeric vector,
\code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}} for the full combination tibble with product detail.
}
