% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd_price_lookup.R
\name{dmd_price_lookup}
\alias{dmd_price_lookup}
\title{Look up medicine prices from a dm+d database}
\usage{
dmd_price_lookup(
  query,
  db = dmdprices::dmd_master,
  method = c("partial", "exact", "fuzzy"),
  max_dist = 3,
  active_only = TRUE
)
}
\arguments{
\item{query}{A character string to search for in medicine names.}

\item{db}{A \verb{<dmd_db>} object from \code{\link[=dmd_load]{dmd_load()}}, or a tibble with the
same columns as \link{dmd_master}. Defaults to the bundled \link{dmd_master} dataset.}

\item{method}{One of:
\itemize{
\item \code{"partial"} \emph{(default)} — case-insensitive substring match using a
regular expression. Suitable for general searching, e.g. \code{"metformin"}.
\item \code{"exact"} — case-insensitive exact match against the full VMP name.
\item \code{"fuzzy"} — approximate string matching (optimal string alignment
distance via \code{\link[stringdist:stringdist]{stringdist::stringdist()}}). Tolerates typos. Tune
sensitivity with \code{max_dist}.
}}

\item{max_dist}{Maximum edit distance for \code{method = "fuzzy"} (default \code{3}).
Increase for looser matching; decrease for stricter matching.}

\item{active_only}{If \code{TRUE} (default), rows where both \code{basic_price} and
\code{nhs_indicative_price} are \code{NA} are dropped.}
}
\value{
A \link[tibble:tibble]{tibble} with the following columns:\tabular{ll}{
   Column \tab Description \cr
   \code{medicine} \tab VMP (generic) name \cr
   \code{pack_size} \tab Pack quantity \cr
   \code{unit} \tab Unit of measure (tablet, ml, etc.) \cr
   \code{vmp_snomed_code} \tab VMP SNOMED CT identifier \cr
   \code{vmpp_snomed_code} \tab VMPP SNOMED CT identifier \cr
   \code{drug_tariff_category} \tab e.g. "Part VIIIA Category M" \cr
   \code{basic_price} \tab Drug Tariff basic price (pence) \cr
   \code{nhs_indicative_price} \tab NHS Indicative Price (pence) \cr
   \code{price_basis} \tab Basis of NHS Indicative Price \cr
   \code{price_date} \tab Date of NHS Indicative Price \cr
   \code{ampp_name} \tab Branded pack name \cr
   \code{ampp_snomed_code} \tab AMPP SNOMED CT identifier \cr
}
}
\description{
Searches a dm+d pricing table for medicines whose names match \code{query}.
Returns a tibble in the same column format as the NHS Drug Tariff Part VIIIA
CSV, with Drug Tariff and NHS Indicative Price columns appended.
}
\details{
By default, the bundled \link{dmd_master} dataset (Week 34 2025, 14 August 2025)
is used, so no setup is needed. Supply \code{db} to use a more recent release
loaded with \code{\link[=dmd_load]{dmd_load()}}.
}
\examples{
# Uses bundled data — no setup required
dmd_price_lookup("metformin")

dmd_price_lookup("Metformin 500mg tablets", method = "exact")

dmd_price_lookup("metfromin 500mg tablets", method = "fuzzy", max_dist = 4)

# Include rows without any price
dmd_price_lookup("metformin", active_only = FALSE)

\dontrun{
# Use a locally loaded, more recent release
db <- dmd_load("~/dmdDataLoader")
dmd_price_lookup("metformin", db = db)
}
}
