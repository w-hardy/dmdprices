% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmd_dose_optimise.R
\name{dmd_dose_cost}
\alias{dmd_dose_cost}
\title{Vectorised dose cost lookup}
\usage{
dmd_dose_cost(
  query,
  dose,
  dose_unit = NULL,
  db = dmdprices::dmd_master,
  method = c("partial", "exact", "fuzzy"),
  max_dist = 3,
  price = c("basic_price", "nhs_indicative_price"),
  objective = c("cheapest", "min_items"),
  preparation = NULL,
  active_only = TRUE,
  can_split = TRUE,
  can_split_vials = FALSE,
  na_value = NA_real_
)
}
\arguments{
\item{query, dose_unit, db, method, max_dist, price, preparation, active_only, can_split}{As in \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}.}

\item{objective}{Character vector of one or more of \code{"cheapest"},
\code{"min_items"}, \code{"most_expensive"}, or \code{"all"}. The cost returned per dose
element is aggregated across preparation groups using each objective's
natural extremum (minimum for \code{"cheapest"} / \code{"min_items"}; maximum for
\code{"most_expensive"}). When multiple objectives are supplied, the
\strong{minimum} of the per-objective aggregates is returned — i.e. the
default \code{c("cheapest", "min_items")} returns the cheapest achievable cost,
while \code{"most_expensive"} alone returns the worst-case cost across groups.}

\item{can_split_vials}{As in \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}. If \code{TRUE}, vials and
ampoules are costed as a fraction of a container (vial sharing).}

\item{dose}{A \strong{numeric vector} of dose values in \code{dose_unit}. \code{NA}, zero,
or negative elements are returned as \code{na_value} without error.}

\item{na_value}{Scalar returned for doses that are \code{NA}, non-positive, or for
which no solution is found. Default \code{NA_real_}.}
}
\value{
A \code{numeric} vector of length \code{length(dose)} giving the dose cost in
pence. Use \verb{/ 100} for GBP. \code{NA} (or \code{na_value}) where no solution exists.
}
\description{
A lightweight, vectorised alternative to \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}} designed for
costing large tables. Accepts a numeric vector of doses and returns a numeric
vector of costs in pence of the same length.
}
\details{
Unlike \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}, this function:
\itemize{
\item Calls the memoized candidate preparation step \strong{once} regardless of
how many doses are supplied.
\item Applies unit-matching and \code{preparation} filters \strong{once}.
\item Runs only the DP optimisation per dose element, skipping the full
result-assembly (combination tibble, notes, etc.).
\item Returns a plain \code{numeric} vector — not a tibble — suitable for use
directly inside \code{\link[dplyr:mutate]{dplyr::mutate()}}.
}

When multiple preparation groups match (e.g. no \code{preparation} filter is
supplied), the \strong{minimum cost across all groups} is returned for each dose.
}
\examples{
\dontrun{
# Vectorised costing inside a mutate — no map_dbl needed
library(dplyr)
treatment_df |>
  mutate(
    ritux_gbp = dmd_dose_cost(
      query       = "rituximab",
      dose        = day1_ritux_mg,
      dose_unit   = "mg",
      objective   = "cheapest",
      preparation = "infusion"
    ) / 100
  )
}
}
\seealso{
\code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}} for the full result tibble with combination
details.
}
