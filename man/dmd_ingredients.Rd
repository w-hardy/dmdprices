% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{dmd_ingredients}
\alias{dmd_ingredients}
\title{NHS dm+d per-ingredient strengths}
\format{
A tibble with one row per VMP/ingredient and 9 columns:
\describe{
\item{vmp_snomed_code}{\code{character}. SNOMED CT identifier for the VMP.}
\item{ingredient_snomed_code}{\code{character}. SNOMED CT identifier for the
ingredient substance (ISID).}
\item{ingredient_name}{\code{character}. Ingredient substance name,
e.g. \code{"Codeine phosphate"}.}
\item{strength_value}{\code{numeric}. Strength numerator value.}
\item{strength_unit}{\code{character}. Strength numerator unit (e.g. \code{"mg"}).}
\item{denominator_value}{\code{numeric}. Strength denominator value for
concentrations, else \code{NA}.}
\item{denominator_unit}{\code{character}. Strength denominator unit
(e.g. \code{"ml"}), else \code{NA}.}
\item{strength_canonical}{\code{numeric}. Strength in canonical units
(mass in mg, volume in ml, or biological activity as \code{"unit"}), for
cross-product comparison. \code{NA} for strengths recorded in units that have
no mass equivalent (e.g. radioactivity in GBq/MBq, amount of substance in
mmol, vaccine antigen units, or volumes such as microlitre). Such
ingredients cannot be dose-optimised by mass via \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}.}
\item{strength_unit_canon}{\code{character}. Canonical strength unit, or \code{NA}
when the strength has no mass/volume/activity equivalent.}
}
}
\source{
NHS Dictionary of Medicines and Devices (dm+d). Published by the NHS Business
Services Authority (NHSBSA).

© Crown copyright. Contains public sector information licensed under the
\strong{Open Government Licence v3.0}.\cr
\url{https://www.nationalarchives.gov.uk/doc/open-government-licence/version/3/}
}
\usage{
dmd_ingredients
}
\description{
A tidy table of ingredient strengths built from the dm+d Virtual Product
Ingredient (VPI) extract, with one row per (VMP, ingredient). It identifies
the individual active ingredients — and their strengths — of every VMP,
including combination products such as co-codamol, enabling
ingredient-specific dose optimisation via \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}.
}
\details{
A VMP with two or more distinct ingredients is a combination product; this is
also surfaced as the \code{is_combination} column on a \code{\link[=dmd_load]{dmd_load()}} database's
\verb{$master} table.

The version bundled with the package may be \strong{empty}: the VPI extract is an
optional part of a dm+d release and is not always present. Rebuild the
bundled data from a release that includes
\code{f_vmp_VpiType.csv} (see \code{data-raw/dmd_master.R}), or
load a full release with \code{\link[=dmd_load]{dmd_load()}}, to populate it. Check with
\code{nrow(dmd_ingredients)}.
}
\seealso{
\link{dmd_master}, \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}, \code{\link[=dmd_load]{dmd_load()}}
}
\keyword{datasets}
