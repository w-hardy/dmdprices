% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_dmd_db.R
\name{as_dmd_db}
\alias{as_dmd_db}
\title{Build a dm+d database from an in-memory table}
\usage{
as_dmd_db(master, ingredients = NULL, loaded_at = Sys.time())
}
\arguments{
\item{master}{A data frame with, at minimum, a \code{medicine} column and one of
\code{basic_price} / \code{nhs_indicative_price} (in pence). Columns already matching
the \code{dmdprices} schema are used as-is; missing canonical columns are filled.}

\item{ingredients}{Optional. \code{NULL} (default), or a data frame of
per-ingredient strengths in the \link{dmd_ingredients} shape, enabling
ingredient-targeted dose optimisation.}

\item{loaded_at}{A length-1 \code{POSIXct} timestamp recording when the data was
assembled. Defaults to \code{\link[=Sys.time]{Sys.time()}}.}
}
\value{
A \verb{<dmd_db>} object: a list with \verb{$master} (a \link[tibble:tibble]{tibble}
in the canonical schema), \verb{$ingredients} (the supplied table or \code{NULL}), and
\verb{$loaded_at} (the timestamp).
}
\description{
Turn a data frame of medicine prices into a \verb{<dmd_db>} object that the
\code{dmdprices} functions (\code{\link[=dmd_price_lookup]{dmd_price_lookup()}}, \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}},
\code{\link[=dmd_dose_cost]{dmd_dose_cost()}}, \code{\link[=dmd_master_info]{dmd_master_info()}}) accept via their \code{db} argument.

This is the supported entry point for \strong{external} Drug-Tariff-shaped data
(for example the \code{drug_tariff_viii_a} table from the NICE \code{COSTmos} package).
Rename your columns to the \code{dmdprices} schema first — the same columns the
bundled \link{dmd_master} carries and that \code{\link[=dmd_load]{dmd_load()}} produces — then pass the
frame here. Missing optional columns are filled with \code{NA} and column types are
coerced; a message reports any resulting loss of functionality.
}
\details{
The required column is \code{medicine}, plus at least one price column
(\code{basic_price} or \code{nhs_indicative_price}, both in \strong{pence}). All other
canonical columns — \code{pack_size}, \code{unit}, \code{vmp_snomed_code},
\code{vmpp_snomed_code}, \code{drug_tariff_category}, \code{price_basis}, \code{price_date},
\code{ampp_name}, \code{ampp_snomed_code} — are filled with \code{NA} when absent. See
\link{dmd_master} for the full column contract.

Features degrade predictably when columns are missing: without \code{ampp_name},
brand-name search is disabled (\code{\link[=dmd_price_lookup]{dmd_price_lookup()}} matches the generic name
only); without \code{ampp_snomed_code}, pack identifiers are \code{NA} and
\code{\link[=dmd_master_info]{dmd_master_info()}} reports 0 AMPPs; without \code{pack_size}/\code{unit}, dose
optimisation is unavailable or degraded.

Unlike \code{\link[=dmd_load]{dmd_load()}}, prices are trusted as given — a \code{0} price stays \code{0}
(it is not treated as missing). Leave \code{loaded_at} at its default: the dose
optimiser's session cache keys on it, so a fixed value shared across two
different tables could return a stale cached result.
}
\examples{
# A minimal Drug-Tariff-shaped table (prices in pence).
df <- data.frame(
  medicine = c("Metformin 500mg tablets", "Metformin 1000mg tablets"),
  pack_size = c(28, 28),
  unit = "tablet",
  basic_price = c(58L, 180L),
  nhs_indicative_price = c(63L, 190L)
)
# ampp_name / ampp_snomed_code are absent, so as_dmd_db() warns that
# brand search and pack identifiers are unavailable.
db <- as_dmd_db(df)
dmd_price_lookup("metformin", db = db)

\dontrun{
# Interoperate with NICE COSTmos Drug Tariff Part VIIIA data.
# COSTmos is not on CRAN; install it separately.
library(dplyr)
db <- COSTmos::drug_tariff_viii_a |>
  rename(unit = unit_of_measure, basic_price = basic_price_in_p) |>
  as_dmd_db()
dmd_dose_optimise("metformin", dose = "1500 mg", db = db)
}
}
\seealso{
\code{\link[=dmd_load]{dmd_load()}} to read a full dm+d release from disk;
\code{\link[=dmd_price_lookup]{dmd_price_lookup()}}, \code{\link[=dmd_dose_optimise]{dmd_dose_optimise()}}.
}
