% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_strength.R
\name{dmd_parse_strength}
\alias{dmd_parse_strength}
\title{Parse a dm+d VMP name into drug stem, strength, and remainder}
\usage{
dmd_parse_strength(name)
}
\arguments{
\item{name}{Character vector of VMP names.}
}
\value{
A \link[tibble:tibble]{tibble} with one row per input, with columns:
\code{drug_stem}, \code{strength_value}, \code{strength_unit}, \code{denominator_value},
\code{denominator_unit}, \code{tail}, \code{strength_canonical}, \code{strength_unit_canon},
\code{is_combination} (logical), \code{n_components} (integer count of parsed
ingredients), and \code{components} (a list-column of per-ingredient tibbles
with \code{value}, \code{unit}, \code{canonical_value}, and \code{canonical_unit}). For
combination products \code{strength_value} / \code{strength_canonical} are \code{NA}
because a single scalar strength is not meaningful; use \code{components}.
}
\description{
Extracts a numeric strength and optional per-denominator concentration
(e.g. \code{mg/ml}, \code{microgram/dose}) from a VMP name. Also returns a canonical
form (mass in mg, volume in ml, biological activity as \code{"unit"}).
}
\details{
Strengths written with comma thousands separators, as dm+d does for large
biological-activity values (e.g. nystatin \code{"100,000units/ml"}), parse
identically to their plain forms.

Combination (multi-ingredient) products such as co-codamol
(\code{"8mg/500mg"}) or co-careldopa (\code{"25mg/100mg"}) are detected and their
individual ingredient strengths returned in the \code{components} list-column,
rather than being misread as a single mass-per-mass concentration. A
trailing volume/dose denominator on a combination liquid (e.g. co-trimoxazole
\code{"80mg/400mg/5ml"}) is captured in \code{denominator_value} / \code{denominator_unit}.
}
\examples{
dmd_parse_strength(c(
  "Metformin 500mg tablets",
  "Morphine 10mg/5ml oral solution",
  "Salbutamol 100micrograms/dose inhaler CFC free",
  "Nystatin 100,000units/ml oral suspension"
))

# Combination products expose per-ingredient strengths
res <- dmd_parse_strength("Co-codamol 8mg/500mg tablets")
res$is_combination
res$components[[1]]
}
