# .app_dir() errors when the bundled app is missing

    Code
      .app_dir("dmd_price_lookup")
    Condition
      Error in `.app_dir()`:
      ! Could not find the "dmd_price_lookup" app directory. Try re-installing dmdprices.

